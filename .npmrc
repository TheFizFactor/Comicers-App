msvs_version=2022
python=python3
# Disable file tracking during builds to avoid path issues on Windows
track_file_access=false
# Ensure we skip problematic native modules when possible
node_gyp_force_python=python3
# Set npm registry for faster dependency resolution
registry=https://registry.npmjs.org/